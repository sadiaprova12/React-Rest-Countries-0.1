import React from 'react';

const Header = () => {
    return (
        <div>
            <h2>Welcome to my React Rest Countries</h2>
            <nav>
                <a href="#">Home</a>
                <a href="#">About Us</a>
                <a href="#">Contact Us</a>
            </nav>
        </div>
    );
};

export default Header;